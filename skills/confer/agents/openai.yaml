interface:
  display_name: "Confer"
  short_description: "Consult Claude, Codex, Kimi via Pi, or GPT Pro in resumable threads"
  default_prompt: "Use $confer to get a second opinion on this from a peer model and keep the thread resumable."

policy:
  allow_implicit_invocation: true

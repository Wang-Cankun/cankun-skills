interface:
  display_name: "Repo Wayfinder"
  short_description: "Ground repository docs in current code"
  default_prompt: "Use $repo-wayfinder to audit this repository’s documentation authority and propose the smallest code-grounded wayfinding map."

policy:
  allow_implicit_invocation: true
